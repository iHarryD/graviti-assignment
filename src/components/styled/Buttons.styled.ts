import styled from "styled-components";

export const PrimaryButton = styled.button`
  background: var(--ACCENT-DARK-BLUE-CLR);
  border-radius: 5rem;
  color: #fff;
  padding: clamp(0.5em, 0.4em + 1vw, 0.9em) clamp(1em, 0.8em + 1vw, 1.7em);
`;
